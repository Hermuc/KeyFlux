<Window xmlns="https://github.com/avaloniaui"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:vm="using:KeyFlux.Settings.ViewModels"
        x:Class="KeyFlux.Settings.Views.PluginMarketWindow"
        x:Name="DlgRoot"
        x:DataType="vm:PluginMarketViewModel"
        TransparencyLevelHint="Blur,AcrylicBlur,Transparent,None"
        Background="{DynamicResource ClaudeWindowSurfaceBrush}"
        Title="Setting"
        Width="640" Height="560"
        WindowStartupLocation="CenterOwner">

  <!-- 插件市场 (插件页入口) —— Claude 暖色风格 (DESIGN.md), 与插件页同一配色体系:
       拉取 marketplace.json 目录, 一键下载安装 (走本地导入 API)。
       目录不可达时如实呈现错误 + 重试, 引导本地导入作为替代路径。
       本次(提交 2)补按动/焦点态, 并将硬编码 hex 改为引用皮肤文件 Styles/Skins/Claude.axaml 的资源键。 -->

  <Window.Styles>
    <Style Selector="TextBlock.pageTitle">
      <Setter Property="FontFamily" Value="Georgia, Microsoft YaHei UI" />
      <Setter Property="FontSize" Value="20" />
      <Setter Property="FontWeight" Value="Medium" />
      <Setter Property="Foreground" Value="{StaticResource ClaudeNearBlackBrush}" />
    </Style>
    <Style Selector="TextBlock.pluginName">
      <Setter Property="FontFamily" Value="Georgia, Microsoft YaHei UI" />
      <Setter Property="FontSize" Value="15" />
      <Setter Property="FontWeight" Value="SemiBold" />
      <Setter Property="Foreground" Value="{StaticResource ClaudeNearBlackBrush}" />
    </Style>
    <Style Selector="Border.marketCard">
      <Setter Property="Background" Value="{StaticResource ClaudeIvoryBrush}" />
      <Setter Property="BorderBrush" Value="{StaticResource ClaudeBorderCreamBrush}" />
      <Setter Property="BorderThickness" Value="1" />
      <Setter Property="CornerRadius" Value="{StaticResource ClaudeRadiusLg}" />
      <Setter Property="Padding" Value="18,14" />
      <Setter Property="Margin" Value="0,0,0,10" />
    </Style>
    <Style Selector="Border.marketCard:pointerover">
      <Setter Property="BoxShadow" Value="{StaticResource ClaudeShadowHoverRing}" />
    </Style>
    <Style Selector="Border.marketCard:pressed">
      <Setter Property="BoxShadow" Value="{StaticResource ClaudeShadowPressedInset}" />
    </Style>
    <Style Selector="Border.marketCard:focus-visible">
      <Setter Property="BoxShadow" Value="{StaticResource ClaudeShadowFocusRing}" />
    </Style>
    <Style Selector="Border.marketCard:focus-within">
      <Setter Property="BoxShadow" Value="{StaticResource ClaudeShadowFocusRing}" />
    </Style>
    <Style Selector="Button.cta">
      <Setter Property="Background" Value="{StaticResource ClaudeTerracottaBrush}" />
      <Setter Property="Foreground" Value="{StaticResource ClaudeIvoryBrush}" />
      <Setter Property="CornerRadius" Value="{StaticResource ClaudeRadiusMd}" />
      <Setter Property="Padding" Value="14,6" />
      <Setter Property="RenderTransform" Value="translate(0px, 0px)" />
    </Style>
    <Style Selector="Button.cta:pressed">
      <Setter Property="RenderTransform" Value="translate(0px, 1px)" />
    </Style>
    <Style Selector="Button.cta:pressed /template/ ContentPresenter#PART_ContentPresenter">
      <Setter Property="BoxShadow" Value="{StaticResource ClaudeShadowPressedInset}" />
    </Style>
    <Style Selector="Button.cta:focus-visible /template/ ContentPresenter#PART_ContentPresenter">
      <Setter Property="BoxShadow" Value="{StaticResource ClaudeShadowFocusRing}" />
    </Style>
    <Style Selector="Button.sand">
      <Setter Property="Background" Value="{StaticResource ClaudeSandBrush}" />
      <Setter Property="Foreground" Value="{StaticResource ClaudeCharcoalWarmBrush}" />
      <Setter Property="CornerRadius" Value="{StaticResource ClaudeRadiusMd}" />
      <Setter Property="Padding" Value="12,5" />
      <Setter Property="RenderTransform" Value="translate(0px, 0px)" />
    </Style>
    <Style Selector="Button.sand:pressed">
      <Setter Property="RenderTransform" Value="translate(0px, 1px)" />
    </Style>
    <Style Selector="Button.sand:pressed /template/ ContentPresenter#PART_ContentPresenter">
      <Setter Property="BoxShadow" Value="{StaticResource ClaudeShadowPressedInset}" />
    </Style>
    <Style Selector="Button.sand:focus-visible /template/ ContentPresenter#PART_ContentPresenter">
      <Setter Property="BoxShadow" Value="{StaticResource ClaudeShadowFocusRing}" />
    </Style>
  </Window.Styles>

  <Border Background="Transparent" Padding="20">
    <DockPanel>

      <!-- 底部: 关闭 (611) -->
      <StackPanel DockPanel.Dock="Bottom" Orientation="Horizontal" Spacing="10"
                  HorizontalAlignment="Right" Margin="0,14,0,0">
        <Button Classes="sand" Click="OnCloseClick" Padding="12,6">
          <TextBlock Text="{Binding LanguageTick, Converter={StaticResource Tr}, ConverterParameter=611}" />
        </Button>
      </StackPanel>

      <Grid RowDefinitions="Auto,*,Auto">

        <!-- 标题 (2428 与插件页入口按钮共用) -->
        <TextBlock Grid.Row="0" Classes="pageTitle" Margin="0,0,0,14"
                   Text="{Binding LanguageTick, Converter={StaticResource Tr}, ConverterParameter=2428}" />

        <!-- 目录主体 -->
        <ScrollViewer Grid.Row="1">
          <StackPanel Spacing="10">

            <!-- 加载中 -->
            <TextBlock Text="{Binding LanguageTick, Converter={StaticResource Tr}, ConverterParameter=2439}"
                       Foreground="{StaticResource ClaudeStoneGrayBrush}" IsVisible="{Binding IsLoading}" />

            <!-- 拉取失败: 原因 + 重试 (920) -->
            <Border Background="{StaticResource ClaudeIvoryBrush}" BorderBrush="{StaticResource ClaudeBorderCreamBrush}" BorderThickness="1"
                    CornerRadius="{StaticResource ClaudeRadiusMd}" Padding="14" IsVisible="{Binding ShowError}">
              <StackPanel Spacing="10">
                <TextBlock Text="{Binding LanguageTick, Converter={StaticResource Tr}, ConverterParameter=2433}"
                           FontWeight="SemiBold" Foreground="{StaticResource ClaudeErrorBrush}" TextWrapping="Wrap" />
                <TextBlock Text="{Binding LoadError}" Foreground="{StaticResource ClaudeStoneGrayBrush}" FontSize="12"
                           TextWrapping="Wrap" />
                <Button Classes="sand" Command="{Binding RetryCommand}" HorizontalAlignment="Left">
                  <TextBlock Text="{Binding LanguageTick, Converter={StaticResource Tr}, ConverterParameter=920}" />
                </Button>
              </StackPanel>
            </Border>

            <!-- 目录为空 -->
            <TextBlock Text="{Binding LanguageTick, Converter={StaticResource Tr}, ConverterParameter=2438}"
                       Foreground="{StaticResource ClaudeStoneGrayBrush}" IsVisible="{Binding ShowEmpty}" />

            <!-- 市场条目卡 -->
            <ItemsControl ItemsSource="{Binding Entries}">
              <ItemsControl.ItemTemplate>
                <DataTemplate x:DataType="vm:MarketEntryVm">
                  <Border Classes="marketCard">
                    <Grid ColumnDefinitions="*,Auto">
                      <StackPanel Grid.Column="0" Spacing="4" Margin="0,0,12,0">
                        <StackPanel Orientation="Horizontal" Spacing="8">
                          <TextBlock Classes="pluginName" Text="{Binding DisplayName}"
                                     VerticalAlignment="Center" />
                          <Border Background="{StaticResource ClaudeSandBrush}" CornerRadius="10" Padding="8,2"
                                  VerticalAlignment="Center"
                                  IsVisible="{Binding VersionText, Converter={StaticResource NotEmpty}}">
                            <TextBlock Text="{Binding VersionText}" FontSize="11" Foreground="{StaticResource ClaudeCharcoalWarmBrush}" />
                          </Border>
                        </StackPanel>
                        <TextBlock Text="{Binding Description}" TextWrapping="Wrap"
                                   Foreground="{StaticResource ClaudeOliveGrayBrush}" FontSize="12" />
                        <TextBlock Text="{Binding Author}" Foreground="{StaticResource ClaudeStoneGrayBrush}" FontSize="11"
                                   IsVisible="{Binding Author, Converter={StaticResource NotEmpty}}" />
                      </StackPanel>
                      <!-- 安装 (terracotta CTA) / 已安装 (Warm Sand 徽标, 暖色体系不用绿) -->
                      <Button Grid.Column="1" Classes="cta" VerticalAlignment="Center"
                              Command="{Binding #DlgRoot.((vm:PluginMarketViewModel)DataContext).InstallCommand}"
                              CommandParameter="{Binding}"
                              IsVisible="{Binding !IsInstalled}">
                        <TextBlock Text="{Binding #DlgRoot.((vm:PluginMarketViewModel)DataContext).LanguageTick, Converter={StaticResource Tr}, ConverterParameter=2434}" />
                      </Button>
                      <Border Grid.Column="1" VerticalAlignment="Center"
                              IsVisible="{Binding IsInstalled}"
                              Background="{StaticResource ClaudeSandBrush}" CornerRadius="{StaticResource ClaudeRadiusMd}" Padding="12,6">
                        <TextBlock FontSize="12" Foreground="{StaticResource ClaudeOliveGrayBrush}" FontWeight="SemiBold"
                                   Text="{Binding #DlgRoot.((vm:PluginMarketViewModel)DataContext).LanguageTick, Converter={StaticResource Tr}, ConverterParameter=2435}" />
                      </Border>
                    </Grid>
                  </Border>
                </DataTemplate>
              </ItemsControl.ItemTemplate>
            </ItemsControl>

          </StackPanel>
        </ScrollViewer>

        <!-- 安装回显 -->
        <TextBlock Grid.Row="2" Text="{Binding StatusText}" FontSize="13" Foreground="{StaticResource ClaudeTerracottaBrush}"
                   FontWeight="SemiBold" Margin="0,10,0,0"
                   IsVisible="{Binding StatusText, Converter={StaticResource NotEmpty}}" />

      </Grid>
    </DockPanel>
  </Border>
</Window>
