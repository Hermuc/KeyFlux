<!--
  ============================================================================
  KeyFlux · Claude / Anthropic 暖色共享主题资源字典
  ============================================================================
  本文件 = 全部 Claude 风格配色与复用类样式的唯一真源。
  计划书 (toasty-cascade-darwin-N1t5WwND.md) 第 1.1 / 1.2 节的落地实现。

  >> 令牌 对照 DESIGN.md 表 (值直接照抄 DESIGN.md, 严禁另写一份 hex) <<

    【画刷 18 项 · 浅色(本次接线)】
      ClaudeParchmentBrush    #f5f4ed  Parchment          页面底
      ClaudeIvoryBrush        #faf9f5  Ivory              卡片面
      ClaudeWhiteBrush        #ffffff  Pure White
      ClaudeSandBrush         #e8e6dc  Warm Sand         次级面/徽标
      ClaudeNearBlackBrush    #141413  Near Black        正文/标题
      ClaudeCharcoalWarmBrush #4d4c48  Charcoal Warm     浅暖底上的深字
      ClaudeOliveGrayBrush    #5e5d59  Olive Gray        次级正文
      ClaudeStoneGrayBrush    #87867f  Stone Gray        弱化文字
      ClaudeDarkWarmBrush     #3d3d3a  Dark Warm         文字链接/强调次级
      ClaudeTerracottaBrush   #c96442  Terracotta        主 CTA
      ClaudeCoralBrush        #d97757  Coral             焦点环 + 文字强调
      ClaudeErrorBrush        #b53333  Error Crimson
      ClaudeMutedGreenBrush   #5e7d5a  低饱和暖绿(正向/已映射)      [代定 见 第4节]
      ClaudeMutedGreenSoftBrush #e7ebe3 暖绿浅底(已映射按键填充)   [代定 见 第4节]
      ClaudeBorderCreamBrush  #f0eee6  Border Cream      标准浅底边
      ClaudeBorderWarmBrush   #e8e6dc  Border Warm      强调分隔/窗口描边
      ClaudeRingWarmBrush     #d1cfc5  Ring Warm         卡片 hover
      ClaudeRingDeepBrush     #c2c0b6  Ring Deep         active

    【画刷 4 项 · 暗色桩(定义但不接线, 见文末接线注释)】
      ClaudeDarkSurfaceBrush  #30302e  Dark Surface   暗色容器/导航边
      ClaudeDeepDarkBrush     #141413  Deep Dark      暗色页底
      ClaudeWarmSilverBrush   #b0aea5  Warm Silver    暗底文字
      ClaudeBorderDarkBrush   #30302e  Border Dark    暗底边

    【阴影 12 项 · BoxShadows】
      ClaudeShadowHoverRing    0 0 0 1 #d1cfc5               卡片 :pointerover (Ring Warm)
      ClaudeShadowCtaRing      0 0 0 1 #c96442               主 CTA 静止态环 (DESIGN.md L126)
      ClaudeShadowPressedInset inset 0 0 0 1 #26262526       按钮 :pressed (Level 4; 0x26=15% 深)
      ClaudeShadowFocusRing    0 0 0 2 #d97757               键盘焦点环 (Coral, 2px) [焦点环裁定 见 第4节]
      ClaudeShadowWhisper      0 4 24 0 #0d000000            浮层/对话框/窗口 (Level 3; 0x0d=5% 黑)
      ClaudeShadowCard         0 2 4 0 #1c000000, 0 6 18 0 #14000000
                                                              卡片静止态柔和投影 (双层: 贴边 + 弥散)
                                                              通用卡 (Border.claudeCard 等) 用此档;
                                                              三页组件框自 2026-09-17 起改用下方 Halo 家族
      ClaudeShadowCardHover    0 0 0 1 #d1cfc5, 0 6 20 0 #1a000000
                                                              卡片悬停 Ring 版 (无消费方; 契约保留)
      ClaudeShadowCardDeep     0 2 10 0 #18000000, 0 10 26 0 #2b000000
                                                              卡片悬停去环版 (无消费方; 契约保留)
                                                              ⚠ 悬停加深已于 2026-09-17 取消,
                                                              三页 :pointerover 加深接线删除
      —— 组件框悬停光圈族 (2026-09-17 新增; 三页组件框在为唯一消费方) ——
      ClaudeShadowCardHalo         0 2 4 0 #1c000000, 0 6 18 0 #14000000, 0 0 16 2 #00c96442
                                                            静止档 = 原双层投影 + **alpha=0 的光圈占位层**
                                                            (占位层不可见, 只为与悬停档层数对齐)
      ClaudeShadowCardHaloHover    0 2 4 0 #1c000000, 0 6 18 0 #14000000, 0 0 16 2 #4dc96442
                                                            悬停档: 第 3 层 = 陶土色弥散光圈 (橙色 α30%)
      ClaudeShadowCardHaloPressed  inset 0 0 0 1 #26262526, 0 6 18 0 #00000000, 0 0 16 2 #00c96442
                                                            按下档 (层数同为 3, 供平滑插值)
      ClaudeShadowCardHaloFocus    0 2 4 0 #00000000, 0 6 18 0 #00000000, 0 0 0 2 #d97757
                                                            聚焦档 = 现有 Coral 2px 焦点环前移到第 3 层
                                                            (前两层透明化, 层数与静止档一致)

    【尺寸 6 项 · x:Double】
      ClaudeRadiusSm=6  ClaudeRadiusMd=8  ClaudeRadiusLg=12  ClaudeRadiusXl=16
      ClaudeRadiusCard=14 (页面级组件框)  ClaudeRadiusPanel=4 (嵌套面板/提示条, 组件框两档制)

    【字体 2 项 · FontFamily】
      AppUiFont       = MiSans 全局 UI 字体 (2026-09-13 换, 随应用打包, 见定义处注释)
      ClaudeSerifFont = MiSans (原 Georgia 衬线弃用, 键名契约保留)

  >> 焦点环决策 (计划书 第4节) <<
      DESIGN.md 的 Focus Blue #3898ec 是冷色, 用户指定用暖色系, 故选定 Coral #d97757
      (2px) 补偿辨识度。ClaudeShadowFocusRing 用 Coral, 而非 Focus Blue。

  >> 暗色主题桩接线方式 (本次只定义令牌 + 此注释, 不接线) <<
      本文件内每个态样式若需暗色变体, 再补一份 ThemeVariantScope 双份声明
      (Light 用浅色令牌 / Dark 用 Claude 暗色桩令牌);
      总开关在 App.axaml 的 RequestedThemeVariant=Light, 改为 Default 跟随系统,
      改 Dark 强制暗色。本次不改该开关 (计划书 第8节)。

  >> 接入方式 <<
      App.axaml 通过 StyleInclude 引入本文件 (根节点即 Styles, 其 Styles.Resources
      随 StyleInclude 合并进应用资源字典, 故 StaticResource 令牌在全部页面/窗口中均可解析)。
  ============================================================================

  ############################################################################
  ##  皮肤契约 (Skin Contract) —— 换肤功能的唯一接口面                       ##
  ############################################################################
  本文件是**皮肤 1 (Claude 暖色)**。切换皮肤功能尚未实现, 但配色已完全模块化:
  换肤 = 替换下表中的三处, 无需触碰任何页面 XAML 或 ViewModel。

  【换肤点 · 共 3 处】
    1. 本文件 (Styles/Skins/<皮肤名>.axaml)
       —— 定义下方「必需令牌清单」的全部键 + 全部 claude* 类样式。
    2. App.axaml 的 <StyleInclude> 一行 —— 指向 1 中的文件。(真正的开关)
    3. App.axaml 的 Application.Resources 里 3 个 ToggleSwitch* 覆盖值
       —— 之所以不在本文件: Fluent 模板内部用 {DynamicResource} 取它们,
          而模板资源查找沿【逻辑树】走, 不经过样式集合资源 (与 ScrollBarSize 同理)。
       ⚠ 实际是【11 个】键, 不止 3 个 —— Fluent 只为非悬停态提供了
          ToggleSwitchFillOn / StrokeOn / KnobFillOn 三个键;
          **悬停与按下走的是 *_PointerOver / *_Pressed 变体, 其内置值是硬编码蓝
          (#269fff / #0078d7 / #00589e)**, 只覆盖那三个键会表现为
          「开关平时是陶土色, 鼠标一放上去就变蓝」。
          完整清单: ToggleSwitchFillOn / StrokeOn / KnobFillOn
                  + FillOnPointerOver / StrokeOnPointerOver / KnobFillOnPointerOver
                  + FillOnPressed / StrokeOnPressed / KnobFillOnPressed
                  + FillOffPointerOver / StrokeOffPointerOver / FillOffPressed / StrokeOffPressed
          新皮肤必须整组替换, 且悬停色建议取「比该皮肤焦点色浅一档」的值。
    另: C# 侧色值真源是 ViewModels/ClaudePalette.cs, 新增皮肤需同步提供对应 palette;
        它已被 8 个文件引用 (HotkeyCapture / SelectedActionPageView / NavItem /
        MainViewModel / Keymap·Abbr·CustomHotkey·SelectedAction PageViewModel /
        MappingRowVm / MarkdownRenderer / WindowPickerSession)。

  【必需令牌清单 —— 新皮肤必须提供同名同类型的全部键, 否则 SkinContractTests 会红】
    画刷 (20): ClaudeParchmentBrush / ClaudeIvoryBrush / ClaudeWhiteBrush /
              ClaudeSandBrush / ClaudeNearBlackBrush / ClaudeCharcoalWarmBrush /
              ClaudeOliveGrayBrush / ClaudeStoneGrayBrush / ClaudeDarkWarmBrush /
              ClaudeTerracottaBrush / ClaudeCoralBrush / ClaudeCoralLightBrush / ClaudeErrorBrush /
              ClaudeMutedGreenBrush / ClaudeMutedGreenSoftBrush / ClaudeTerracottaSoftBrush /
              ClaudeBorderCreamBrush / ClaudeBorderWarmBrush /
              ClaudeRingWarmBrush / ClaudeRingDeepBrush /
              暗色桩 (定义但不接线): ClaudeDarkSurfaceBrush / ClaudeDeepDarkBrush /
              ClaudeWarmSilverBrush / ClaudeBorderDarkBrush
    阴影 (12): ClaudeShadowHoverRing / ClaudeShadowCtaRing /
              ClaudeShadowPressedInset / ClaudeShadowFocusRing / ClaudeShadowWhisper /
              ClaudeShadowCard / ClaudeShadowCardHover / ClaudeShadowCardDeep /
              ClaudeShadowCardHalo / ClaudeShadowCardHaloHover /
              ClaudeShadowCardHaloPressed / ClaudeShadowCardHaloFocus
              ⚠ Halo 四档必须层数相等 (各 3 层) —— BoxShadowsAnimator 按 oldValue.Count
                输出层数, 不等会在动画末帧跳变; 且**光圈层 Blur 必须 >0** (Blur==0 即描边线)。
    尺寸 (6):  ClaudeRadiusSm / ClaudeRadiusMd / ClaudeRadiusLg / ClaudeRadiusXl /
              ClaudeRadiusCard / ClaudeRadiusPanel
              ⚠ 类型必须是 CornerRadius (不是 x:Double) —— 历史事故: 曾定义成 Double,
                构建 0 错误但所有窗口加载即抛 InvalidCastException。
    字体 (2):  AppUiFont + ClaudeSerifFont (均 MiSans, 见定义处注释)
    动效 (4):  C# 真源 ViewModels/ClaudeMotion.cs (Press/Micro/Standard/Enter, TimeSpan)
              ⚠ XAML 引用一律 {x:Static vm:ClaudeMotion.*}, 勿写散落字面量
    类样式:    TextBlock.claude{Title,Subtitle,Body,Muted,Dim} /
              Border.claudeCard / Button.claude{Cta,Sand,Ghost,Danger} /
              ToggleSwitch.claudeCompact / TextBox·ComboBox.claudeInput /
              ControlTheme ChipRadio (芯片单选, 契约 = 键存在且 TargetType=RadioButton) /
              ControlTheme DeleteKey (背退格删除键, 契约 = 键存在且 TargetType=Button)

  【本皮肤仍需遵守的设计约束 (来自 DESIGN.md, 换肤时按新皮肤规范替换)】
    · 不得引入高饱和色 (仅允许品牌色做强调)
    · 互动态一律用 ring 阴影 (0 0 0 1) 而非投影; pressed 用 inset 环 (Level 4)
    · 标题用 serif, 字重上限 Medium; 正文行高 1.60
  ############################################################################
-->
<Styles xmlns="https://github.com/avaloniaui"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:vm="using:KeyFlux.Settings.ViewModels">

  <!-- ===================== 令牌 (Styles.Resources) ===================== -->
  <Styles.Resources>

    <!-- 画刷: 浅色(18)
         ⚠ 消费方引用方式约定 (2026-09-22): 各 View 对画刷一律用 {DynamicResource}。
         缘由: 毛玻璃期间为支持运行期覆写曾把 StaticResource 批量换成 DynamicResource;
         2026-09-22 毛玻璃功能整体移除后经 QA 复核**保留未回退** —— 二者在无运行期改写时
         完全等价, 回退 40+ 处属无谓 churn; 且 DynamicResource 为后续运行期换肤/主题切换
         留了扩展口。新代码请沿用 DynamicResource。 -->
    <SolidColorBrush x:Key="ClaudeParchmentBrush"    Color="#f5f4ed" />
    <SolidColorBrush x:Key="ClaudeIvoryBrush"        Color="#faf9f5" />
    <SolidColorBrush x:Key="ClaudeWhiteBrush"        Color="#ffffff" />
    <SolidColorBrush x:Key="ClaudeSandBrush"         Color="#e8e6dc" />
    <SolidColorBrush x:Key="ClaudeNearBlackBrush"    Color="#141413" />
    <SolidColorBrush x:Key="ClaudeCharcoalWarmBrush"  Color="#4d4c48" />
    <SolidColorBrush x:Key="ClaudeOliveGrayBrush"    Color="#5e5d59" />
    <SolidColorBrush x:Key="ClaudeStoneGrayBrush"    Color="#87867f" />
    <SolidColorBrush x:Key="ClaudeDarkWarmBrush"     Color="#3d3d3a" />
    <SolidColorBrush x:Key="ClaudeTerracottaBrush"   Color="#c96442" />
    <SolidColorBrush x:Key="ClaudeCoralBrush"        Color="#d97757" />
    <SolidColorBrush x:Key="ClaudeCoralLightBrush"   Color="#e1957a" />
    <SolidColorBrush x:Key="ClaudeErrorBrush"        Color="#b53333" />
    <SolidColorBrush x:Key="ClaudeMutedGreenBrush"   Color="#5e7d5a" />
    <SolidColorBrush x:Key="ClaudeMutedGreenSoftBrush" Color="#e7ebe3" />
    <SolidColorBrush x:Key="ClaudeTerracottaSoftBrush" Color="#f6e4da" />
    <SolidColorBrush x:Key="ClaudeBorderCreamBrush"  Color="#f0eee6" />
    <SolidColorBrush x:Key="ClaudeBorderWarmBrush"   Color="#e8e6dc" />
    <SolidColorBrush x:Key="ClaudeRingWarmBrush"     Color="#d1cfc5" />
    <SolidColorBrush x:Key="ClaudeRingDeepBrush"     Color="#c2c0b6" />

    <!-- 画刷: 暗色桩(4) · 定义但不接线 -->
    <SolidColorBrush x:Key="ClaudeDarkSurfaceBrush"  Color="#30302e" />
    <SolidColorBrush x:Key="ClaudeDeepDarkBrush"     Color="#141413" />
    <SolidColorBrush x:Key="ClaudeWarmSilverBrush"   Color="#b0aea5" />
    <SolidColorBrush x:Key="ClaudeBorderDarkBrush"   Color="#30302e" />

    <!-- 阴影 (12) · BoxShadows
         ClaudeShadowCard/CardHover (2026-09-11): 插件卡静止态此前只有 1px cream 细边,
         用户反馈"阴影边界紧贴内容" —— 补双层柔和投影 (贴边 + 弥散) 让卡片与画布拉开层次。
         悬停必须在 Ring 之外保留投影: BoxShadow 是单属性, 悬停若只给 Ring 会把静止投影整个替换掉。
         ⚠ 首版 (8%/6% alpha, blur 10) 在浅米底上肉眼不可见, 用户复验"没有变化" ——
         浅色背景上的卡片投影 alpha 要 ≥10% 量级才可感知, 现为 11%/8% + blur 18。 -->
    <BoxShadows x:Key="ClaudeShadowHoverRing">0 0 0 1 #d1cfc5</BoxShadows>
    <BoxShadows x:Key="ClaudeShadowCtaRing">0 0 0 1 #c96442</BoxShadows>
    <BoxShadows x:Key="ClaudeShadowPressedInset">inset 0 0 0 1 #26262526</BoxShadows>
    <BoxShadows x:Key="ClaudeShadowFocusRing">0 0 0 2 #d97757</BoxShadows>
    <BoxShadows x:Key="ClaudeShadowWhisper">0 4 24 0 #0d000000</BoxShadows>
    <BoxShadows x:Key="ClaudeShadowCard">0 2 4 0 #1c000000, 0 6 18 0 #14000000</BoxShadows>
    <!-- ClaudeShadowCardHover: 卡片悬停 "Ring + 略深投影" 档。
         ⚠ **无接线消费方** (2026-09-17 起): 其 1px #d1cfc5 零扩散环层会在悬停时画出灰线
         (与卡片实体描边叠成双线), 用户明确要求清除; 三页卡片先改走下方去环版
         ClaudeShadowCardDeep, 最终**取消悬停加深**, 悬停一律保持静止档 ClaudeShadowCard。
         令牌保留在皮肤契约 (SkinContractTests 有列名守护 + 无环层断言), 作为恢复路径。 -->
    <BoxShadows x:Key="ClaudeShadowCardHover">0 0 0 1 #d1cfc5, 0 6 20 0 #1a000000</BoxShadows>
    <!-- ClaudeShadowCardDeep 去环版 (2026-09-13 定义; 2026-09-17 两度调整后**取消接线**):
         曾作为三页组件框悬停的唯一影档 (描边走实体 BorderThickness 的消费方不能带环 ——
         1px 环会与实体描边叠成双线), 但用户对加深幅度的三轮反馈 (不够明显 → 过于明显 →
         再降重仍不满意) 最终导向「取消悬停投影加深效果, 保留基础阴影」。
         ⇒ 三页 :pointerover 投影接线已整体删除 (见各 View 注释), 悬停回落静止档 ClaudeShadowCard。
         本令牌**当前无消费方**, 保留仅为留一条恢复路径; 契约只剩"不得含零扩散环层"
         (SkinContractTests.Skin_Contract_Card_Shadow_Tokens_Contain_No_Ring_Layer)。
         当前值 = 最后一次"求柔和"调整的产物 (与静止档同族双层, 墨量 26.3% / 下坠 10 / 弥散 26,
         实测卡下 Δ亮度 ≈0.086 —— 仍被用户判为偏重, 这正是取消加深的直接原因)。
         若将来要恢复悬停加深, 建议从更轻的配方起步 (墨量 ≤1.25× 静止档), 并保留下面三条不变式:
           ① 不得含 `0 0 0 N` 零扩散层 (否则复现悬停灰线);
           ② 下坠 ≥ 1.4× 静止 (层次感来自位移);
           ③ 最大弥散 ≥ 静止档 (减重靠"摊薄"而非"收紧", 否则边缘生硬)。 -->
    <BoxShadows x:Key="ClaudeShadowCardDeep">0 2 10 0 #18000000, 0 10 26 0 #2b000000</BoxShadows>

    <!-- ══════════ 弹窗玻璃材质 (2026-09-24 苹果式弹窗动效批次) ══════════
         "像有重量的半透明玻璃从背景中浮到前景" —— 三件套:
           · 连续圆角: ClaudeRadiusXl (16px), 比页面卡片 (14) 大一档, 强调"独立浮起的面板"
           · 柔和阴影: ClaudeShadowDialog —— 三层结构 (贴边 / 中层 / 大弥散),
             层数比 Card 家族多一层, 因为弹窗要压过"已被遮罩压暗 + 模糊"的背景才有浮起感
           · 细微边缘高光: ClaudeDialogEdgeHighlight 画刷 —— 顶边一道浅暖白细线,
             模拟玻璃上沿受光 (这是"玻璃感"最廉价也最有效的一笔)

         ⚠ 层数契约 (同 Halo 家族的教训): 若将来给弹窗阴影加过渡动画, BoxShadowsAnimator
           在 progress<1 时按 oldValue.Count 输出层数 ⇒ 参与动画的影档必须层数相等,
           否则多出的层会在动画结束帧突然出现 (表现为"投影两段跳")。当前弹窗阴影不做过渡
           (入场用 RenderTransform + Opacity, 不碰 BoxShadow), 故暂无此约束;
           一旦引入, 请照 Halo 家族的做法用 alpha=0 占位层对齐。 -->
    <BoxShadows x:Key="ClaudeShadowDialog">0 1 2 0 #1f000000, 0 8 20 0 #1c000000, 0 20 48 0 #24000000</BoxShadows>

    <!-- 玻璃边缘高光: 顶边一道浅暖白 1px (受光面), 与实体描边叠加形成"厚度感"。
         取 0.55 alpha 的暖白 —— 再高会读成"白边", 再低看不出。 -->
    <SolidColorBrush x:Key="ClaudeDialogEdgeHighlightBrush" Color="#8cffffff" />

    <!-- ── 组件框悬停光圈 (ClaudeShadowCardHalo 家族, 2026-09-17 新增) ──
         需求: 三页 (选中动作 / 插件 / Settings) 的组件框悬停时周围显示"一圈光圈"。
         硬约束: **不能是纯线条**; 颜色优先橙色, 由实现方判断取最协调者。

         ▶ 为什么这么实现 (而不是加一圈描边)
           零扩散环层 (`0 0 0 N #...`, Blur=0) 画出来就是一根**描边线** ——
           2026-09-17 早先的"悬停灰描边"事故正是它 (`0 0 0 1 #d1cfc5`, 与卡片 2px 实体奶油边
           叠成双线), 用户已明令清除。故光圈用**无偏移 + 大模糊 + 低不透明度**的弥散层成形:
             layer3 = `0 0 16 0 <#aarrggbb>` —— offset 恒 0 ⇒ 四周等量外扩;
                                              Blur=16 ⇒ 约 8px 的渐隐尾巴, 无生硬边缘。
           SkinContractTests 对 Halo/HaloHover 有"光圈层必须 Blur>0"的反线条断言 (Blur==0 即红)。

         ▶ 颜色为何选 Terracotta #c96442
           Coral #d97757 是**键盘焦点环专用色** (ClaudeShadowFocusRing, 2px 实环), 亮度更高,
           在浅米底上做弥散对比不足, 且会与焦点态语义打架 (悬停=橙光晕 / 焦点=橙实环, 需可区分);
           Terracotta 是品牌主 CTA 色, 更深一档, 同族暖橙既满足"橙色优先", 又与 Parchment/Ivory
           同源和谐。第 3 层取 `0 0 16 2` (无偏移 / Blur 16 / Spread 2): Spread 2 把光圈从卡沿
           **推出 2px** 再让 Blur 16 化开 ⇒ 卡边一圈约 8px 的橙色渐隐带 (有"圈"的形, 无"线"的硬)。

         ▶ alpha 标定 (2026-09-17 实测两轮)
           首版 0x2e (18%) 经 Skia 截帧实测卡下 2..7 行 Δ亮度仅 0.0134 (暖度 Δ(R−B)=3.33) ——
           与历史"看不出变化"的档 (Δ0.0085) 太近, 偏弱; 现提为 0x4d (30%)。
           量级参照 (本项目实测库): Δ0.0085 = 用户判"没有变化"; Δ0.074 起 = 用户判"过于明显"。
           光圈是**彩色**层, 同 Δ 下的感知强度高于黑色投影 (色相变化本身可辨), 故收敛目标
           取黑投影"过重线"之下的一半量级附近。改 alpha 后必须跑
           SettingsCardEffectTests.Hover_Halo_Lights_Warm_Pixels_Below_Card 复测 (用例内含
           0.004 下界与 0.12 上界两道闸, 兼顾"看不见"与"过重"两种历史翻车)。
           注: 弥散层的可见峰值只约源 alpha 的一半 (另一半落在卡片下方被卡面遮住),
           故弥散比普通投影需要更高的源 alpha 才等效。

         ▶ ⚠ 四档必须**层数相等 (各 3 层)** —— 这不是风格问题, 是动画正确性
           Avalonia 的 BoxShadowsAnimator 在 progress<1 时按 **oldValue.Count** 输出层数:
             `int cnt = progress >= 1d ? newValue.Count : oldValue.Count;`
           层数不等 ⇒ 多出来的层在动画**结束的那一帧**才突然出现/消失 (悬停移出时表现为
           "投影两段跳")。故静止档用一个 alpha=0 的同形光圈层占位, 与悬停/按下/聚焦档逐层对齐:
             L0 = 原贴边影 (HaloFocus 里透明化, 与 Coral 实环的 morph 由 L2 承担)
             L1 = 原弥散影 (同上)
             L2 = 光圈层 (静止 alpha=0 → 悬停 18% → 按下/聚焦回落 0)
           改本族任何一个 alpha / blur 时, **四个键必须一起看**, 并保持层数一致。

         ▶ 接线位置 (三页, 2026-09-17)
           SelectedActionPageView `Border.actionCard` (含 row-card) / PluginsPageView
           `Border.pluginCard` / SettingsPageView `Border.settingsCard` + `Border.leftPanel`,
           均为 `:pointerover` → HaloHover + `BoxShadowsTransition` (ClaudeMotion.Micro 120ms)。
           页面内不再出现旧的 :pointerover 加深接线; 旧档 CardHover / CardDeep 仍无消费方。 -->
    <BoxShadows x:Key="ClaudeShadowCardHalo">0 2 4 0 #1c000000, 0 6 18 0 #14000000, 0 0 16 2 #00c96442</BoxShadows>
    <BoxShadows x:Key="ClaudeShadowCardHaloHover">0 2 4 0 #1c000000, 0 6 18 0 #14000000, 0 0 16 2 #4dc96442</BoxShadows>
    <BoxShadows x:Key="ClaudeShadowCardHaloPressed">inset 0 0 0 1 #26262526, 0 6 18 0 #00000000, 0 0 16 2 #00c96442</BoxShadows>
    <BoxShadows x:Key="ClaudeShadowCardHaloFocus">0 2 4 0 #00000000, 0 6 18 0 #00000000, 0 0 0 2 #d97757</BoxShadows>

    <!-- 尺寸 (4) · CornerRadius 类型
         注意: 令牌被赋给 CornerRadius 属性, 必须用 CornerRadius 类型。
         若用 x:Double, 运行时对 CornerRadius 属性赋值会抛 InvalidCastException
         (构建通过但启动即崩, 见 QA 反馈)。故用四角统一值的 CornerRadius 元素。 -->
    <CornerRadius x:Key="ClaudeRadiusSm">6,6,6,6</CornerRadius>
    <CornerRadius x:Key="ClaudeRadiusMd">8,8,8,8</CornerRadius>
    <CornerRadius x:Key="ClaudeRadiusLg">12,12,12,12</CornerRadius>
    <CornerRadius x:Key="ClaudeRadiusXl">16,16,16,16</CornerRadius>
    <!-- 组件框两档制 (2026-09-18 用户裁定): 描边/光圈类 bug 高发于圆角不一致 ——
         嵌套卡半径不等 ⇒ halo 弧段外溢; 大半径弧在 Viewbox 分数缩放下线宽失真。
         凡"组件框"(卡片容器, 不含 toggle/chip/按钮/输入框) 只许用这两档:
         Card = 页面级组件框 (明确需要大圆角), Panel = 嵌套面板/提示条 (接近直角)。 -->
    <CornerRadius x:Key="ClaudeRadiusCard">14,14,14,14</CornerRadius>
    <CornerRadius x:Key="ClaudeRadiusPanel">4,4,4,4</CornerRadius>

    <!-- 字体 (2) · FontFamily -->
    <!-- AppUiFont: 全局 UI 字体单源 (2026-09-13 用户指定 MiSans, 随应用打包,
         4 字重入 Assets/Fonts, 族名均 "MiSans")。放皮肤而非 App.axaml ——
         测试宿主不加载 App.axaml (坑 40), StaticResource 会炸; 各 XAML 经
         {StaticResource AppUiFont} 引用。链尾 YaHei 兜缺字, Emoji 垫尾。
         例外 (刻意保留): 等宽 Consolas (代码/键盘布局), MDI 图标, Twemoji 表情 -->
    <FontFamily x:Key="AppUiFont">avares://KeyFlux.Settings/Assets/Fonts/#MiSans, Microsoft YaHei UI, Segoe UI Emoji</FontFamily>
    <!-- ClaudeSerifFont: 原 Georgia 衬线, 2026-09-13 全局换 MiSans (用户指定)。
         键名为皮肤契约锁定保留 (历史语义"衬线"已不存在, 勿据名猜测用途) -->
    <FontFamily x:Key="ClaudeSerifFont">avares://KeyFlux.Settings/Assets/Fonts/#MiSans, Microsoft YaHei UI</FontFamily>

    <!-- ── 动效时长令牌 (6) ── C# 强类型真源 = ViewModels/ClaudeMotion.cs (与色值真源
         ClaudePalette.cs 同一先例; TimeSpan 无法作 XAML 资源元素实例化, AVLN2000 实测),
         XAML 一律经 {x:Static vm:ClaudeMotion.*} 引用。皮肤动效个性的单一真源:
         新皮肤按自身气质整组替换 (例: 高效皮肤整体减半)。
         两类令牌 (SkinContractTests 分别锁界):
           交互反馈类 Press/Micro/Standard/Enter ≤300ms, 递增序 = 按压<微交互<标准<入场;
           内容揭示类 Roll/Unroll ≤600ms, 递增序 = 卷起<摊开 (2026-09-24 用户裁定「卷轴缓缓摊开」,
           2026-09-24 用户多轮提速后定为 摊开/卷起 各 100ms; 两向共用 SineEaseInOut 保证节奏对称连贯)。
         减少动效: 环境变量 KEYFLUX_NO_MOTION=1 时设置页整体跳过入场/摊开 (MotionPreferences)。 -->


  <!-- ToolTip 无动画直显 (2026-09-13 用户报: 淡入动画结束才显示完整文字)。覆盖 Fluent
       ControlTheme: 原版 Opacity 0 + 0.15s DoubleTransition + :open 才变 1; 本版恒 Opacity=1
       无 Transitions —— 悬浮即完整显示。模板/配色资源与 Fluent 原版逐项一致, 仅去动画。
       注意: ControlTheme 必须放 Styles.Resources, 作 Styles 直接子项会 AVLN3000 (实测) -->
  <!-- ToolTip 无动画直显 (2026-09-13 用户报: 先淡入动画结束才显示完整文字)。
       覆盖 Fluent 的 ToolTip ControlTheme: 原版 Opacity 0 + 0.15s DoubleTransition +
       :open 才变 1 (淡入); 本版恒 Opacity=1 无 Transitions —— 悬浮即完整显示。
       模板/配色资源与 Fluent 原版逐项一致 (DynamicResource 主题键), 仅去动画。 -->
  <ControlTheme x:Key="{x:Type ToolTip}" TargetType="ToolTip">
    <Setter Property="Foreground" Value="{DynamicResource ToolTipForeground}" />
    <Setter Property="Background" Value="{DynamicResource ToolTipBackground}" />
    <Setter Property="BorderBrush" Value="{DynamicResource ToolTipBorderBrush}" />
    <Setter Property="BorderThickness" Value="{DynamicResource ToolTipBorderThemeThickness}" />
    <Setter Property="FontSize" Value="{DynamicResource ToolTipContentThemeFontSize}" />
    <Setter Property="Padding" Value="{DynamicResource ToolTipBorderThemePadding}" />
    <Setter Property="MaxWidth" Value="320" />
    <Setter Property="CornerRadius" Value="{DynamicResource OverlayCornerRadius}" />
    <Setter Property="Opacity" Value="1" />
    <Setter Property="Template">
      <ControlTemplate>
        <Border Name="PART_LayoutRoot"
                BorderThickness="{TemplateBinding BorderThickness}"
                Background="{TemplateBinding Background}"
                BorderBrush="{TemplateBinding BorderBrush}"
                Padding="{TemplateBinding Padding}"
                CornerRadius="{TemplateBinding CornerRadius}">
          <ContentPresenter Name="PART_ContentPresenter"
                            MaxWidth="{TemplateBinding MaxWidth}"
                            Content="{TemplateBinding Content}"
                            ContentTemplate="{TemplateBinding ContentTemplate}"
                            TextBlock.TextWrapping="Wrap"/>
        </Border>
      </ControlTemplate>
    </Setter>
  </ControlTheme>

  <!-- ── 芯片单选 (ChipRadio, 2026-09-13) ──
       行为选择器的卡片式单选 (对齐 Semi CardRadioButton 模式): 整块可点 (热区=整个芯片,
       替代裸圆圈单选的小热区), 悬停暖灰描边+Ivory 底, 选中 Terracotta 软底+Terracotta 描边,
       键盘焦点 Coral 描边, 按下下沉 1px。完整自绘模板 (只有自 Border, 不依赖 Fluent 模板
       部件 —— 规避「模板部件压属性须逐状态覆盖」坑)。消费方 = ActionEditorPanel 枚举单选;
       用法: <RadioButton Theme="{StaticResource ChipRadio}" GroupName="..."/> -->
  <ControlTheme x:Key="ChipRadio" TargetType="RadioButton">
    <Setter Property="Background" Value="{StaticResource ClaudeWhiteBrush}" />
    <Setter Property="BorderBrush" Value="{StaticResource ClaudeBorderWarmBrush}" />
    <Setter Property="BorderThickness" Value="1" />
    <Setter Property="CornerRadius" Value="{StaticResource ClaudeRadiusMd}" />
    <Setter Property="Padding" Value="12,6" />
    <Setter Property="MinHeight" Value="34" />
    <Setter Property="FontSize" Value="13" />
    <Setter Property="Foreground" Value="{StaticResource ClaudeCharcoalWarmBrush}" />
    <Setter Property="HorizontalAlignment" Value="Stretch" />
    <Setter Property="RenderTransform" Value="translateY(0px)" />
    <Setter Property="Transitions">
      <Transitions>
        <TransformOperationsTransition Property="RenderTransform" Duration="{x:Static vm:ClaudeMotion.Press}" Easing="QuadraticEaseOut" />
        <BrushTransition Property="Background" Duration="{x:Static vm:ClaudeMotion.Micro}" />
        <BrushTransition Property="BorderBrush" Duration="{x:Static vm:ClaudeMotion.Micro}" />
        <BrushTransition Property="Foreground" Duration="{x:Static vm:ClaudeMotion.Micro}" />
      </Transitions>
    </Setter>
    <Setter Property="Template">
      <ControlTemplate>
        <Border Name="PART_Root"
                Background="{TemplateBinding Background}"
                BorderBrush="{TemplateBinding BorderBrush}"
                BorderThickness="{TemplateBinding BorderThickness}"
                CornerRadius="{TemplateBinding CornerRadius}"
                Padding="{TemplateBinding Padding}">
          <ContentPresenter Name="PART_ContentPresenter"
                            Content="{TemplateBinding Content}"
                            ContentTemplate="{TemplateBinding ContentTemplate}"
                            Foreground="{TemplateBinding Foreground}"
                            VerticalAlignment="Center" />
        </Border>
      </ControlTemplate>
    </Setter>
    <Style Selector="^:pointerover">
      <Setter Property="Background" Value="{StaticResource ClaudeIvoryBrush}" />
      <Setter Property="BorderBrush" Value="{StaticResource ClaudeRingDeepBrush}" />
    </Style>
    <Style Selector="^:pressed">
      <Setter Property="RenderTransform" Value="translateY(1px)" />
    </Style>
    <Style Selector="^:checked">
      <Setter Property="Background" Value="{StaticResource ClaudeTerracottaSoftBrush}" />
      <Setter Property="BorderBrush" Value="{StaticResource ClaudeTerracottaBrush}" />
      <Setter Property="Foreground" Value="{StaticResource ClaudeDarkWarmBrush}" />
    </Style>
    <!-- 选中态悬停保持选中外观 (仅描边加深一档), 避免态间来回闪跳 -->
    <Style Selector="^:checked:pointerover">
      <Setter Property="Background" Value="{StaticResource ClaudeTerracottaSoftBrush}" />
      <Setter Property="BorderBrush" Value="{StaticResource ClaudeTerracottaBrush}" />
    </Style>
    <Style Selector="^:focus-visible">
      <Setter Property="BorderBrush" Value="{StaticResource ClaudeCoralBrush}" />
    </Style>
    <Style Selector="^:checked:focus-visible">
      <Setter Property="BorderBrush" Value="{StaticResource ClaudeCoralBrush}" />
    </Style>
    <Style Selector="^:disabled">
      <Setter Property="Opacity" Value="0.4" />
    </Style>
  </ControlTheme>

  <!-- ── 删除按钮 (DeleteKey, 2026-09-13 用户定稿: 只是要粗壮方正的大 X 本体) ──
       一~四版误做成"背退格键形底板+小 X"被连续否决 —— 用户要的是 X 本身:
       粗描边 (3.8) 方头 (SquareCap, 对齐示意图的方正端点) 大 X, 危险红 (符合 UI 配色);
       悬停/键盘焦点浮现比 X 浅的圆角方圈光圈 (TerracottaSoft), 按下下沉 1px,
       禁用降透明。自绘模板零 Fluent 部件依赖; 路径坐标归零 (坑 46)。
       消费面 = 全部 7 处红 ✕: 设置页 3 / 选中动作页 2 / 插件页 1 / QuickSwitch 弹窗 1。
       用法: <Button Theme="{StaticResource DeleteKey}" Command=.../> -->
  <ControlTheme x:Key="DeleteKey" TargetType="Button">
    <Setter Property="Width" Value="20" />
    <Setter Property="Height" Value="20" />
    <Setter Property="VerticalAlignment" Value="Center" />
    <Setter Property="Foreground" Value="{StaticResource ClaudeErrorBrush}" />
    <Setter Property="RenderTransform" Value="translateY(0px)" />
    <Setter Property="Transitions">
      <Transitions>
        <TransformOperationsTransition Property="RenderTransform" Duration="{x:Static vm:ClaudeMotion.Press}" Easing="QuadraticEaseOut" />
      </Transitions>
    </Setter>
    <Setter Property="Template">
      <ControlTemplate>
        <Grid Width="20" Height="20">
          <Border Name="PART_Halo" CornerRadius="6" Background="Transparent" />
          <!-- X 本体: 10.2×10.2 (描边 2.8 + 方头外扩), 危险红 -->
          <Path Data="M 0,0 L 7.4,7.4 M 7.4,0 L 0,7.4"
                Stroke="{TemplateBinding Foreground}" StrokeThickness="2.8"
                StrokeLineCap="Square" StrokeJoin="Miter" Stretch="None"
                HorizontalAlignment="Center" VerticalAlignment="Center" />
        </Grid>
      </ControlTemplate>
    </Setter>
    <Style Selector="^ /template/ Border#PART_Halo">
      <Setter Property="Transitions">
        <Transitions>
          <BrushTransition Property="Background" Duration="{x:Static vm:ClaudeMotion.Micro}" />
        </Transitions>
      </Setter>
    </Style>
    <Style Selector="^:pointerover /template/ Border#PART_Halo">
      <Setter Property="Background" Value="{StaticResource ClaudeTerracottaSoftBrush}" />
    </Style>
    <Style Selector="^:focus-visible /template/ Border#PART_Halo">
      <Setter Property="Background" Value="{StaticResource ClaudeTerracottaSoftBrush}" />
    </Style>
    <Style Selector="^:pressed">
      <Setter Property="RenderTransform" Value="translateY(1px)" />
    </Style>
    <Style Selector="^:disabled">
      <Setter Property="Opacity" Value="0.35" />
    </Style>
  </ControlTheme>
</Styles.Resources>

  <!-- ===================== 复用类样式 (12) ===================== -->

  <!-- 文字层级 -->
  <Style Selector="TextBlock.claudeTitle">
    <Setter Property="FontFamily" Value="{StaticResource ClaudeSerifFont}" />
    <Setter Property="FontSize" Value="24" />
    <Setter Property="FontWeight" Value="Medium" />
    <Setter Property="Foreground" Value="{StaticResource ClaudeNearBlackBrush}" />
  </Style>
  <Style Selector="TextBlock.claudeSubtitle">
    <Setter Property="FontFamily" Value="{StaticResource ClaudeSerifFont}" />
    <Setter Property="FontSize" Value="15" />
    <Setter Property="FontWeight" Value="SemiBold" />
    <Setter Property="Foreground" Value="{StaticResource ClaudeNearBlackBrush}" />
  </Style>
  <Style Selector="TextBlock.claudeBody">
    <Setter Property="FontSize" Value="14" />
    <Setter Property="Foreground" Value="{StaticResource ClaudeNearBlackBrush}" />
  </Style>
  <Style Selector="TextBlock.claudeMuted">
    <Setter Property="FontSize" Value="13" />
    <Setter Property="Foreground" Value="{StaticResource ClaudeOliveGrayBrush}" />
  </Style>
  <Style Selector="TextBlock.claudeDim">
    <Setter Property="FontSize" Value="11" />
    <Setter Property="Foreground" Value="{StaticResource ClaudeStoneGrayBrush}" />
  </Style>

  <!-- 卡片: Ivory 面 + Cream 细边 + 悬停 Ring 阴影 -->
  <Style Selector="Border.claudeCard">
    <Setter Property="Background" Value="{StaticResource ClaudeIvoryBrush}" />
    <Setter Property="BorderBrush" Value="{StaticResource ClaudeBorderCreamBrush}" />
    <Setter Property="BorderThickness" Value="1" />
    <Setter Property="CornerRadius" Value="{StaticResource ClaudeRadiusCard}" />
  </Style>
  <Style Selector="Border.claudeCard:pointerover">
    <Setter Property="BoxShadow" Value="{StaticResource ClaudeShadowHoverRing}" />
  </Style>

  <!-- 主 CTA: Terracotta 底 + Ivory 字。
       BoxShadow 在 Avalonia 11.3 仅存在于 Border / ContentPresenter (不在 Button),
       故阴影一律设在模板 ContentPresenter#PART_ContentPresenter 上;
       Background 亦须同时重设该部件, 否则被 Fluent 模板值压住 (见 1.2 坑 1)。 -->
  <Style Selector="Button.claudeCta">
    <Setter Property="Background" Value="{StaticResource ClaudeTerracottaBrush}" />
    <Setter Property="Foreground" Value="{StaticResource ClaudeIvoryBrush}" />
    <Setter Property="CornerRadius" Value="{StaticResource ClaudeRadiusMd}" />
    <Setter Property="Padding" Value="14,7" />
    <Setter Property="RenderTransform" Value="translate(0px, 0px)" />
    <!-- 三态微动效: 按压下沉 80ms / 悬停变色 120ms (时长令牌见 Styles.Resources 动效组) -->
    <Setter Property="Transitions">
      <Transitions>
        <TransformOperationsTransition Property="RenderTransform" Duration="{x:Static vm:ClaudeMotion.Press}" Easing="QuadraticEaseOut" />
        <BrushTransition Property="Foreground" Duration="{x:Static vm:ClaudeMotion.Micro}" />
        <BrushTransition Property="BorderBrush" Duration="{x:Static vm:ClaudeMotion.Micro}" />
      </Transitions>
    </Setter>
  </Style>
  <Style Selector="Button.claudeCta /template/ ContentPresenter#PART_ContentPresenter">
    <Setter Property="BoxShadow" Value="{StaticResource ClaudeShadowCtaRing}" />
    <!-- 悬停底色 Terracotta→Coral 平滑过渡 (可见的是模板部件底色, 必须在部件上挂过渡) -->
    <Setter Property="Transitions">
      <Transitions>
        <BrushTransition Property="Background" Duration="{x:Static vm:ClaudeMotion.Micro}" />
      </Transitions>
    </Setter>
  </Style>
  <Style Selector="Button.claudeCta:pointerover">
    <Setter Property="Background" Value="{StaticResource ClaudeCoralBrush}" />
  </Style>
  <Style Selector="Button.claudeCta:pointerover /template/ ContentPresenter#PART_ContentPresenter">
    <Setter Property="Background" Value="{StaticResource ClaudeCoralBrush}" />
  </Style>
  <!-- 按动: inset 环 + 轻微下沉 (Level 4) -->
  <Style Selector="Button.claudeCta:pressed">
    <Setter Property="RenderTransform" Value="translate(0px, 1px)" />
  </Style>
  <Style Selector="Button.claudeCta:pressed /template/ ContentPresenter#PART_ContentPresenter">
    <Setter Property="Background" Value="{StaticResource ClaudeCoralBrush}" />
    <Setter Property="BoxShadow" Value="{StaticResource ClaudeShadowPressedInset}" />
  </Style>
  <!-- 焦点环 (必须声明在 :pressed 之后, 见 1.2 坑 2; 二者均写 BoxShadow, 靠声明顺序定胜负) -->
  <Style Selector="Button.claudeCta:focus-visible /template/ ContentPresenter#PART_ContentPresenter">
    <Setter Property="BoxShadow" Value="{StaticResource ClaudeShadowFocusRing}" />
  </Style>

  <!-- 次级按钮: Warm Sand 底 + Charcoal Warm 字 -->
  <Style Selector="Button.claudeSand">
    <Setter Property="Background" Value="{StaticResource ClaudeSandBrush}" />
    <Setter Property="Foreground" Value="{StaticResource ClaudeCharcoalWarmBrush}" />
    <Setter Property="CornerRadius" Value="{StaticResource ClaudeRadiusMd}" />
    <Setter Property="Padding" Value="14,7" />
    <Setter Property="BorderBrush" Value="{StaticResource ClaudeBorderWarmBrush}" />
    <Setter Property="BorderThickness" Value="1" />
    <Setter Property="RenderTransform" Value="translate(0px, 0px)" />
    <Setter Property="Transitions">
      <Transitions>
        <TransformOperationsTransition Property="RenderTransform" Duration="{x:Static vm:ClaudeMotion.Press}" Easing="QuadraticEaseOut" />
        <BrushTransition Property="Foreground" Duration="{x:Static vm:ClaudeMotion.Micro}" />
        <BrushTransition Property="BorderBrush" Duration="{x:Static vm:ClaudeMotion.Micro}" />
      </Transitions>
    </Setter>
  </Style>
  <Style Selector="Button.claudeSand:pointerover">
    <Setter Property="BorderBrush" Value="{StaticResource ClaudeRingWarmBrush}" />
  </Style>
  <Style Selector="Button.claudeSand:pointerover /template/ ContentPresenter#PART_ContentPresenter">
    <Setter Property="BoxShadow" Value="{StaticResource ClaudeShadowHoverRing}" />
  </Style>
  <Style Selector="Button.claudeSand:pressed">
    <Setter Property="RenderTransform" Value="translate(0px, 1px)" />
  </Style>
  <Style Selector="Button.claudeSand:pressed /template/ ContentPresenter#PART_ContentPresenter">
    <Setter Property="BoxShadow" Value="{StaticResource ClaudeShadowPressedInset}" />
  </Style>
  <Style Selector="Button.claudeSand:focus-visible /template/ ContentPresenter#PART_ContentPresenter">
    <Setter Property="BoxShadow" Value="{StaticResource ClaudeShadowFocusRing}" />
  </Style>

  <!-- 幽灵按钮: 透明底 + Border Warm 描边 + Olive 字 -->
  <Style Selector="Button.claudeGhost">
    <Setter Property="Background" Value="Transparent" />
    <Setter Property="Foreground" Value="{StaticResource ClaudeOliveGrayBrush}" />
    <Setter Property="CornerRadius" Value="{StaticResource ClaudeRadiusMd}" />
    <Setter Property="Padding" Value="14,7" />
    <Setter Property="BorderBrush" Value="{StaticResource ClaudeBorderWarmBrush}" />
    <Setter Property="BorderThickness" Value="1" />
    <Setter Property="RenderTransform" Value="translate(0px, 0px)" />
    <Setter Property="Transitions">
      <Transitions>
        <TransformOperationsTransition Property="RenderTransform" Duration="{x:Static vm:ClaudeMotion.Press}" Easing="QuadraticEaseOut" />
        <BrushTransition Property="Foreground" Duration="{x:Static vm:ClaudeMotion.Micro}" />
        <BrushTransition Property="BorderBrush" Duration="{x:Static vm:ClaudeMotion.Micro}" />
      </Transitions>
    </Setter>
  </Style>
  <Style Selector="Button.claudeGhost:pointerover">
    <Setter Property="Foreground" Value="{StaticResource ClaudeDarkWarmBrush}" />
    <Setter Property="BorderBrush" Value="{StaticResource ClaudeRingDeepBrush}" />
  </Style>
  <Style Selector="Button.claudeGhost:pressed">
    <Setter Property="RenderTransform" Value="translate(0px, 1px)" />
  </Style>
  <Style Selector="Button.claudeGhost:pressed /template/ ContentPresenter#PART_ContentPresenter">
    <Setter Property="BoxShadow" Value="{StaticResource ClaudeShadowPressedInset}" />
  </Style>
  <Style Selector="Button.claudeGhost:focus-visible /template/ ContentPresenter#PART_ContentPresenter">
    <Setter Property="BoxShadow" Value="{StaticResource ClaudeShadowFocusRing}" />
  </Style>

  <!-- 危险按钮: 透明底 + Error Crimson 字 -->
  <Style Selector="Button.claudeDanger">
    <Setter Property="Background" Value="Transparent" />
    <Setter Property="Foreground" Value="{StaticResource ClaudeErrorBrush}" />
    <Setter Property="CornerRadius" Value="{StaticResource ClaudeRadiusMd}" />
    <Setter Property="Padding" Value="14,7" />
    <Setter Property="BorderThickness" Value="0" />
    <Setter Property="RenderTransform" Value="translate(0px, 0px)" />
    <Setter Property="Transitions">
      <Transitions>
        <TransformOperationsTransition Property="RenderTransform" Duration="{x:Static vm:ClaudeMotion.Press}" Easing="QuadraticEaseOut" />
        <BrushTransition Property="Foreground" Duration="{x:Static vm:ClaudeMotion.Micro}" />
      </Transitions>
    </Setter>
  </Style>
  <Style Selector="Button.claudeDanger:pointerover">
    <Setter Property="BorderBrush" Value="{StaticResource ClaudeErrorBrush}" />
    <Setter Property="BorderThickness" Value="1" />
  </Style>
  <Style Selector="Button.claudeDanger:pressed">
    <Setter Property="RenderTransform" Value="translate(0px, 1px)" />
  </Style>
  <Style Selector="Button.claudeDanger:pressed /template/ ContentPresenter#PART_ContentPresenter">
    <Setter Property="BoxShadow" Value="{StaticResource ClaudeShadowPressedInset}" />
  </Style>
  <Style Selector="Button.claudeDanger:focus-visible /template/ ContentPresenter#PART_ContentPresenter">
    <Setter Property="BoxShadow" Value="{StaticResource ClaudeShadowFocusRing}" />
  </Style>

  <!-- 紧凑开关: 无右侧文字 (状态文字由卡内 TextBlock 承载)。
       开启态 Terracotta 画刷由消费页面/窗口通过 Fluent 资源键
       (ToggleSwitchFillOn / ToggleSwitchStrokeOn / ToggleSwitchKnobFillOn)
       覆盖, 这些键是资源而非 CLR 属性, 无法在 Style 内设定,
       参照 PluginsPageView.axaml 已有的同款资源键覆盖。 -->
  <Style Selector="ToggleSwitch.claudeCompact">
    <Setter Property="MinWidth" Value="0" />
    <Setter Property="OnContent" Value="{x:Null}" />
    <Setter Property="OffContent" Value="{x:Null}" />
  </Style>

  <!-- 输入框 / 下拉: White 底 + Cream 边 + 焦点 Coral 环 (2026-09-13 底色 Ivory→White:
       消费方是 Ivory 卡片上的面板输入区, White 底与卡面拉开层次, Fluent 默认 TextBox
       亦是 White 底, 面板内未挂类的 AutoCompleteBox 观感自然一致)。
       TextBox/ComboBox 均无 BoxShadow 属性:
       - TextBox 焦点环阴影设在模板 Border#PART_BorderElement 上 (Fluent 实测部件名)。
       - ComboBox 模板无 PART_Border 部件, 无法挂 BoxShadow 环; 焦点态改用
         BorderBrush 变 Coral (模板 Border 经 TemplateBinding 继承), 给出可见焦点指示。
       BorderBrush 均设在控件自身 (经 TemplateBinding 继承到模板 Border)。 -->
  <Style Selector="TextBox.claudeInput">
    <Setter Property="Background" Value="{StaticResource ClaudeWhiteBrush}" />
    <Setter Property="Foreground" Value="{StaticResource ClaudeNearBlackBrush}" />
    <Setter Property="BorderBrush" Value="{StaticResource ClaudeBorderCreamBrush}" />
    <Setter Property="CornerRadius" Value="{StaticResource ClaudeRadiusMd}" />
  </Style>
  <Style Selector="TextBox.claudeInput:focus-visible">
    <Setter Property="BorderBrush" Value="{StaticResource ClaudeCoralBrush}" />
  </Style>
  <Style Selector="TextBox.claudeInput:focus-visible /template/ Border#PART_BorderElement">
    <Setter Property="BoxShadow" Value="{StaticResource ClaudeShadowFocusRing}" />
  </Style>
  <Style Selector="ComboBox.claudeInput">
    <Setter Property="Background" Value="{StaticResource ClaudeWhiteBrush}" />
    <Setter Property="Foreground" Value="{StaticResource ClaudeNearBlackBrush}" />
    <Setter Property="BorderBrush" Value="{StaticResource ClaudeBorderCreamBrush}" />
    <Setter Property="CornerRadius" Value="{StaticResource ClaudeRadiusMd}" />
  </Style>
  <Style Selector="ComboBox.claudeInput:focus-visible">
    <Setter Property="BorderBrush" Value="{StaticResource ClaudeCoralBrush}" />
  </Style>

  <!-- 全局开关状态文字 ON/OFF (2026-09-13 用户要求全大写统一): 未显式设置 OnContent/OffContent
       的 ToggleSwitch (设置页毛玻璃卡等) 由此生效; 行内 rowSwitch (x:Null) 与
       插件页 compact (x:Null+独立 TextBlock) 局部样式优先级更高, 不受影响 -->
  <Style Selector="ToggleSwitch">
    <Setter Property="OnContent" Value="ON" />
    <Setter Property="OffContent" Value="OFF" />
  </Style>

  <!-- 下拉弹层项: 选中/悬停高亮内缩为圆角块 (2026-09-13 用户报弹层方正违和)。
       Fluent 默认 CornerRadius=0 + Margin=0 → 整行色带顶满弹层边缘;
       ComboBoxItem 模板无 Border, 高亮画在 PART_ContentPresenter 上,
       其 CornerRadius 经 TemplateBinding 绑定控件值 (headless 实证)。 -->
  <Style Selector="ComboBoxItem">
    <Setter Property="CornerRadius" Value="6" />
    <Setter Property="Margin" Value="6,3" />
  </Style>

  <!-- ===================== 管理类弹窗共享类 (2026-09-15 新增) =====================
       动机: 「匹配类型」与「行为库」两个管理类弹窗此前各自写一份本地样式, 圆角与配色与设计
       系统脱节 —— 列表沿用 Fluent 默认的冷灰悬停 + 强调色整行选中, 与暖色 UI 违和。
       收敛成皮肤类后两窗共用同一套: 圆角层级 = 容器 Lg(12) / 行·按钮·输入 Md(8) / 徽标 Sm(6);
       配色 = Ivory 面 · Sand 悬停 · Parchment 选中 · Terracotta 指示条。 -->

  <!-- 提示条: Sand 面 + Panel 档圆角 (组件框两档制, 2026-09-18) (原在选中动作页与匹配类型页各写一份本地 .hint) -->
  <Style Selector="Border.claudeHint">
    <Setter Property="Background" Value="{StaticResource ClaudeSandBrush}" />
    <Setter Property="CornerRadius" Value="{StaticResource ClaudeRadiusPanel}" />
    <Setter Property="Padding" Value="10,8" />
  </Style>

  <!-- 管理列表: 去 Fluent 的方底与边线, 容器面交给外层 Border.claudeCard -->
  <Style Selector="ListBox.claudeList">
    <Setter Property="Background" Value="Transparent" />
    <Setter Property="BorderThickness" Value="0" />
    <Setter Property="Padding" Value="6" />
  </Style>

  <!-- 列表行 (作用域 = ListBox.claudeList 的后代): 圆角块 + 内缩 (与 ComboBoxItem 同一做法: 高亮内缩为圆角块, 不顶满容器)。
       悬停 = 暖沙, 选中 = Parchment + 左侧 3px 陶土指示条 (inset 阴影, 不改模板结构)。
       Background 必须同时压 PART_ContentPresenter —— Fluent 模板部件的底色会盖住控件级值。 -->
  <Style Selector="ListBox.claudeList ListBoxItem">
    <Setter Property="CornerRadius" Value="{StaticResource ClaudeRadiusMd}" />
    <Setter Property="Margin" Value="0,0,0,2" />
    <Setter Property="Padding" Value="10,7" />
    <Setter Property="Background" Value="Transparent" />
    <Setter Property="BorderThickness" Value="0" />
  </Style>
  <Style Selector="ListBox.claudeList ListBoxItem:pointerover /template/ ContentPresenter#PART_ContentPresenter">
    <Setter Property="Background" Value="{StaticResource ClaudeSandBrush}" />
  </Style>
  <Style Selector="ListBox.claudeList ListBoxItem:selected /template/ ContentPresenter#PART_ContentPresenter">
    <Setter Property="Background" Value="{StaticResource ClaudeParchmentBrush}" />
    <Setter Property="BoxShadow" Value="inset 3 0 0 0 #c96442" />
  </Style>
  <Style Selector="ListBox.claudeList ListBoxItem:pressed /template/ ContentPresenter#PART_ContentPresenter">
    <Setter Property="Background" Value="{StaticResource ClaudeRingWarmBrush}" />
  </Style>
  <!-- 焦点环须最后声明 (:pressed 与 :focus-visible 同写 BoxShadow, 靠声明顺序定胜负; 见 1.2 坑 2) -->
  <Style Selector="ListBox.claudeList ListBoxItem:focus-visible /template/ ContentPresenter#PART_ContentPresenter">
    <Setter Property="BoxShadow" Value="{StaticResource ClaudeShadowFocusRing}" />
  </Style>

  <!-- ══════════════════ 弹窗玻璃材质 (2026-09-24 苹果式弹窗动效批次) ══════════════════
       .dlgMotion 由 Services/DialogMotion.cs 在弹窗 Opened 时挂到 window.Content 上
       (不是包层 —— 包层会打破既有布局断言, 见该文件注释)。此处只负责"材质", 姿势
       (缩放/位移/透明度) 由 C# 侧跨帧驱动, 两者互不干扰。

       ▶ 为什么圆角/阴影写在皮肤而不是 DialogMotion.cs
         材质是"皮肤个性"的一部分 (换肤应整体替换), 与色值/圆角/阴影令牌同源;
         动效时序才是行为, 留在 C# 真源。这条分工与 ClaudeMotion/ClaudePalette 的既定先例一致。 -->
  <!-- 弹窗玻璃材质 (圆角/阴影) **不在皮肤里设** —— 各弹窗内容根类型不一 (Border / DockPanel /
       ScrollViewer 包裹的命名空间标签), 而 CornerRadius/BoxShadow 只有 Border 具备,
       用选择器适配会碎成一堆类型分支。改由 Services/DialogMotion.cs 在代码侧统一设置:
       若内容根是 Border 则直接设材质; 否则包一层 Border (仅这一情形, 且只在代码侧,
       不改 XAML 结构)。此处只保留与类型无关的两条。 -->

  <!-- 顶边受光高光 (细微边缘高光): 一条 1px 半透明暖白细线, 模拟玻璃上沿受光。
       由 DialogMotion 在包层时同步注入, 类名 .dlgEdge 仅作标记与测试缝。 -->
  <Style Selector="Border.dlgEdge">
    <Setter Property="Height" Value="1" />
    <Setter Property="Background" Value="{StaticResource ClaudeDialogEdgeHighlightBrush}" />
    <Setter Property="VerticalAlignment" Value="Top" />
    <Setter Property="Margin" Value="12,0,12,0" />
    <Setter Property="CornerRadius" Value="1,1,0,0" />
  </Style>

  <!-- 按钮按下轻微缩放反馈 (用户要求"按钮按下有轻微缩放反馈")。
       ⚠ 选择器限定 .dlgMotion 后代 ⇒ 只对**弹窗内**按钮生效, 不给主界面全部按钮引入
       额外的 RenderTransform 通道 (主界面按钮已有各自的 :pressed 底色反馈)。
       过渡由 Services/DialogMotion.cs 的 AttachPressFeedback 在代码侧挂
       (XAML 里 Setter 内嵌 <Transitions> 复合元素需 x:SetterTargetType, 故不走 XAML)。 -->
  <Style Selector="Button.dlgPress">
    <Setter Property="RenderTransform" Value="scale(1)" />
  </Style>
  <Style Selector="Button.dlgPress:pressed">
    <Setter Property="RenderTransform" Value="scale(0.97)" />
  </Style>

</Styles>
